<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.com.hotplace.member">
	<select id="SEARCHLIST" parameterType="java.util.Map"
		resultType="project.com.hotplace.member.model.MemberVO">
		SELECT *
		FROM (
		SELECT t.*, ROWNUM AS row_num
		FROM (
		SELECT * FROM member
		WHERE
		<if test="searchKey == 'NICK_NAME'">
			NICK_NAME
		</if>
		<if test="searchKey == 'EMAIL'">
			EMAIL
		</if>
		 LIKE #{searchWord}
		) t
		)
		WHERE row_num BETWEEN #{start} AND #{end}
	</select>
	<select id="SELECTALL" parameterType="java.util.Map"
		resultType="project.com.hotplace.member.model.MemberVO">
		SELECT *
		FROM (
		SELECT sub.*, ROWNUM AS row_num
		FROM (
		SELECT
		t.*
		FROM member t
		ORDER BY NUM
		) sub
		)
		WHERE row_num BETWEEN #{start,
		jdbcType=BIGINT} AND #{end, jdbcType=BIGINT}
	</select>
	<select id="NICK_NAME_CHECK"
		resultType="project.com.hotplace.member.model.MemberVO">
		SELECT nick_name FROM member WHERE nick_name = #{nick_name}
	</select>
	<select id="EMAIL_CHECK"
		resultType="project.com.hotplace.member.model.MemberVO">
		SELECT email FROM member WHERE email = #{email}
	</select>
	<insert id="INSERTOK"
		parameterType="project.com.hotplace.member.model.MemberVO">
		INSERT INTO
		member(num,nick_name,email,pw,address,gender,food_like)
		VALUES(mem_sequence.nextval,#{nick_name},#{email},#{pw},#{address},#{gender},#{food_like})
	</insert>
	<select id="SELECTONE" resultType="project.com.hotplace.member.model.MemberVO">
		select * from member where num = #{num}
	</select>


	<!-- <select id="LOGIN" resultType="project.com.hotplace.member.model.MemberVO"> -->
	<!-- select * from member where id = #{id} and pw = #{pw} -->
	<!-- </select> -->

	<!-- <select id="M_SEARCH_LIST_NAME" resultType="project.com.hotplace.member.model.MemberVO"> -->
	<!-- select * from member where name like #{searchWord} -->
	<!-- </select> -->

	<!-- <select id="M_SEARCH_LIST_TEL" resultType="project.com.hotplace.member.model.MemberVO"> -->
	<!-- select * from member where tel like #{searchWord} -->
	<!-- </select> -->


	<!-- <update id="M_UPDATE" parameterType="project.com.hotplace.member.model.MemberVO"> -->
	<!-- update member set pw=#{pw} , name=#{name} , tel=#{tel} , save_name=#{save_name} -->
	<!-- where num = #{num} -->
	<!-- </update> -->

	<!-- <delete id="M_DELETE"> -->
	<!-- delete from member where num = #{num} -->
	<!-- </delete> -->

</mapper>